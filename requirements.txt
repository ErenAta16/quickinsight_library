# Core dependencies
pandas>=1.5.0
# Pin NumPy <2 to avoid binary wheels incompatibilities on Windows (e.g., matplotlib built against NumPy 1.x)
numpy>=1.21.0,<2
# Keep matplotlib compatible with NumPy<2 baseline
matplotlib>=3.5.0,<3.8
seaborn>=0.12.0
plotly>=5.10.0
scipy>=1.9.0

# Performance optimization
numba>=0.57.0
dask[complete]>=2022.8.0
# ML baseline
scikit-learn>=1.1.0,<1.6

# GPU acceleration (optional)
cupy-cuda11x>=11.0.0

# Cloud integration (optional)
boto3>=1.26.0
azure-storage-blob>=12.16.0
google-cloud-storage>=2.8.0

# Profiling and monitoring
psutil>=5.9.0

# Development dependencies
pytest>=7.0
pytest-cov>=4.0
black>=22.0
flake8>=5.0
mypy>=1.0
